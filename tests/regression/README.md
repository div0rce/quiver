# Regression tests

One permanent test file per fixed defect (REQ-TEST-011): named `reg_<issue>_<slug>.cpp`,
referencing the GitHub issue and the violated requirement ID in its header comment.
Regression tests are never deleted, only explicitly deprecated with a recorded reason.

Empty at v0.1 — no defects have been fixed post-release yet.
