# Quiver static-analysis configuration — REQ-STD-007 (docs/prd/17-coding-standards.md).
# Check set is pinned; per-check suppressions only via inline NOLINT(check) + reason.
---
Checks: >
  -*,
  bugprone-*,
  performance-*,
  portability-*,
  modernize-use-bit-cast,
  cppcoreguidelines-init-variables,
  misc-definitions-in-headers,
  -bugprone-easily-swappable-parameters,
  -portability-simd-intrinsics
WarningsAsErrors: ''
HeaderFilterRegex: '(include/quiver|src)/.*'
FormatStyle: file
CheckOptions:
  - key: bugprone-easily-swappable-parameters.MinimumLength
    value: '3'
# Note: bugprone-easily-swappable-parameters is disabled globally with reason:
# kernel signatures are pointer+length shaped by contract (REQ-API-* / ADR-006);
# the check would flag the entire public surface. Recorded here per REQ-STD-007.
# Note: portability-simd-intrinsics is disabled globally with reason: it exists to
# suggest std::experimental::simd over raw intrinsics, but first-party per-ISA
# intrinsics ARE the charter decision (no SIMD abstraction layer; Charter §7.2,
# ADR-003) — every *_avx2/_avx512/_neon.cpp TU is intentionally non-portable and
# architecture-guarded. Recorded here per REQ-STD-007 (added at M4).
