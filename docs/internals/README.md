# Internals

Implementation notes written for reviewers and contributors, not consumers: CPU detection tables (M1), the dispatch state machine and its memory-ordering argument (M1), kernel-common utilities and LUTs (M4), the UB catalog (M3), coding standards mirror (M1).

Empty at M0 by design; pages arrive with their subsystems.

Owner: per owning module.
